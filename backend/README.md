# backend

Backend API design and implementation will live here.

Current phase: no backend implementation.

The backend will eventually expose APIs for `Today's Picks`, stock detail explanations, reports, and system metadata.
