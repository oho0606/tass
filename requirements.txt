# TASS core dependencies (see pyproject.toml for full groups)
pandas>=2.0
numpy>=1.24
yfinance>=0.2
pyarrow>=14.0
pydantic>=2.0
pydantic-settings>=2.0
pyyaml>=6.0
loguru>=0.7

# Dev: pip install -e ".[dev]"
# API:  pip install -e ".[api]"
# Data: pip install -e ".[data]"  (pykrx, tenacity, sqlalchemy, redis, apscheduler)
