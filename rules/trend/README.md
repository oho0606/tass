# Deprecated

This directory is **deprecated**. Use [rules/tr/](../tr/) for TR (Trend) category rules.

Legacy `TREND-*` Rule IDs are aliased to `TR-*` per [registry.yaml](../registry.yaml) and [TASS-011](../docs/TASS-011_Rule_Taxonomy_Specification.md).
